apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-persistence-volume-claim
spec:
  resources:
    requests:
      storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
